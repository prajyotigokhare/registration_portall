pipeline 
{
    agent any 
     tools {
     }
     
      stages {
        stage('pull code') {
           steps {
           git credentialsId: '123', url: 'https://prajyotii@bitbucket.org/fs-bitbucket/fsemp.git'
               }
             }  
       
  }
}